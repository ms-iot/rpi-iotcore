# Raspberry Pi VCOS generic Windows kernel mode library

This library is the Windows kernel mode version of VCOS generic library that is
also available in userland. Only minimal component is ported over.